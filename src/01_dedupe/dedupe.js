/* // --- Directions
Given an array of strings with duplicates, return a new array with unique values.
--- Example
  dedupe(['a', 'b', 'c', 'b', 'a']) === ['a', 'b', 'c']
 */
function dedupe(arr) {}

module.exports = dedupe;
